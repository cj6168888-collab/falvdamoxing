# 法律大模型开发宪法

## 证据完整性最高原则

> **任何时候、任何模块，证据的每一个字符都不得被截断、遗漏或选择性忽略。**
>
> 这是一条不可违背的铁律。法律案件中，一个字符的遗漏可能导致整个案件走向截然不同的结果。

---

## 第一章：证据完整性原则

### 1.1 核心定义

**证据**：指在法律案件中提交的所有材料，包括但不限于：
- 合同协议
- 函件书信
- 支付凭证
- 身份证明
- 视听资料
- 证人证言
- 鉴定意见
- 任何其他与案件相关的文字材料

**完整内容**：指证据的原始文本经过提取后的全部字符，不得进行任何删减、截断、摘要或选择性忽略。

### 1.2 不可违背的铁律

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                          │
│   ⚠️  铁律：证据完整性优先原则                                            │
│                                                                          │
│   1. 证据内容不得截断                                                      │
│   2. 证据内容不得摘要                                                      │
│   3. 证据内容不得选择性忽略                                                │
│   4. 证据中的每一个字符都必须传递给AI进行分析                              │
│   5. 任何分析都必须基于证据的完整内容                                       │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 第二章：禁止行为清单

### 2.1 严格禁止的截断操作

| 禁止操作 | 错误示例 | 正确做法 |
|---------|---------|---------|
| 字符数截断 | `content[:50000]` | `content` |
| 字符数截断 | `content[:200]` | `content` |
| 字符数截断 | `text[:500]` | `text` |
| 字符数截断 | `summary[:50000]` | `summary` |
| 摘要截断 | `text[:5000] + "..."` | `text` |
| 省略号截断 | `content[:500] + "..."` | `content` |
| 尾部省略 | `summary[:50000] + content[-5000:]` | `content` |

### 2.2 严格禁止的内容筛选

| 禁止操作 | 错误示例 | 正确做法 |
|---------|---------|---------|
| 首段提取 | 只保留前200字符 | 保留完整内容 |
| 关键词筛选 | 只保留含关键词的段落 | 保留所有段落 |
| 智能提取 | 首段 + 高分段落 + 末段 | 保留完整内容 |
| 权重过滤 | 只保留高分内容 | 保留所有内容 |

### 2.3 严格禁止的数量限制

| 禁止操作 | 错误示例 | 正确做法 |
|---------|---------|---------|
| 证据数量限制 | `evidence[:5]` | `evidence` |
| 证据数量限制 | `evidence[:50]` | `evidence` |
| 摘要长度限制 | `summary[:500]` | `summary` |

---

## 第三章：允许的操作

### 3.1 允许的格式规范化

以下操作是允许的，因为它们不改变内容本身：

- 去除首尾空白：`content.strip()`
- 换行符统一：`content.replace("\r\n", "\n")`
- 规范化编码：`content.encode().decode('utf-8')`

### 3.2 允许的显示优化

- 在用户界面的预览区域显示部分内容（仅用于预览，不用于分析）
- 在日志中显示部分内容（仅用于调试，不影响处理）
- 在缓存键生成时使用内容哈希（不影响内容本身）

### 3.3 允许的非证据内容处理

- **案件描述**：可以适当截断，但建议保留完整内容
- **法律条文**：可以适当截断，但建议保留完整内容
- **用户输入**：不受此限制
- **分析结果输出**：不受此限制

---

## 第四章：代码规范

### 4.1 证据内容获取优先级

```python
# 正确做法：按优先级使用完整内容
content = (
    evidence.extracted_content or  # 优先使用提取的完整内容
    evidence.raw_content or         # 其次使用原始内容
    evidence.summary                 # 最后才考虑摘要（已修复为完整内容）
)
```

### 4.2 证据内容存储

```python
# 正确做法：存储完整内容
evidence.extracted_content = full_text  # 完整文本
evidence.raw_content = full_text        # 完整文本
evidence.summary = full_text            # 摘要 = 完整内容（法律应用）
```

### 4.3 证据内容传递给AI

```python
# 正确做法：传递完整内容
prompt = f"""分析以下证据：

【证据名称】{evidence.name}
【证据类型】{evidence.evidence_type}
【证据内容】{evidence.extracted_content or evidence.raw_content}
"""
```

### 4.4 禁止的模式

```python
# ❌ 严格禁止
content[:50000]
content[:200]
content[:500]
summary[:500]
evidence[:5]
evidence[:50]
content[:500] + "..."
text[:5000] + content[-5000:]
```

---

## 第五章：已修复的截断问题

以下文件中的截断问题已全部修复：

### 5.1 核心证据服务

| 文件 | 原问题 | 修复状态 |
|-----|-------|---------|
| `evidence_folder_service.py` | 200字符摘要 | ✅ 已修复 |
| `evidence_v2.py` | 55000字符截断 | ✅ 已修复 |
| `evidence_v2.py` | 证据比较截断 | ✅ 已修复 |
| `evidence_qa.py` | 50000字符截断 | ✅ 已修复 |
| `evidence_context_injector.py` | 50000字符截断 | ✅ 已修复 |
| `evidence_context_injector.py` | 5条证据限制 | ✅ 已修复 |
| `evidence_context_injector.py` | 500字符摘要 | ✅ 已修复 |
| `evidence_three_natures.py` | 摘要优先使用 | ✅ 已修复 |
| `evidence_graph.py` | 50000字符截断 | ✅ 已修复 |
| `evidence_graph.py` | 哈希截断 | ✅ 已修复 |
| `rag_service.py` | 2000字符分块 | ✅ 已修复（增大到5000）|

### 5.2 高级分析服务

| 文件 | 原问题 | 修复状态 |
|-----|-------|---------|
| `senior_lawyer_engine.py` | 50000字符截断 | ✅ 已修复 |
| `smart_qa_v2.py` | 50000字符截断 | ✅ 已修复 |
| `smart_qa_v2.py` | 5条证据限制 | ✅ 已修复 |
| `insight_engine.py` | 50000字符截断 | ✅ 已修复 |
| `legal_protection.py` | 50000字符截断 | ✅ 已修复 |
| `streaming_report.py` | 50000字符截断 | ✅ 已修复 |
| `deadline_service.py` | 50000字符截断 | ✅ 已修复 |
| `evidence_timing.py` | 50000字符截断 | ✅ 已修复 |
| `trap_detector.py` | 50000字符截断 | ✅ 已修复 |
| `milestone_generator.py` | 50000字符截断 | ✅ 已修复 |

### 5.3 API层

| 文件 | 原问题 | 修复状态 |
|-----|-------|---------|
| `api/document.py` | 50000字符截断 | ✅ 已修复 |
| `api/assistant_api.py` | 50000字符截断 | ✅ 已修复 |
| `api/streaming_analysis.py` | 2000字符摘要 | ✅ 已修复 |

### 5.4 辅助模块

| 文件 | 原问题 | 修复状态 |
|-----|-------|---------|
| `legal_db/case_crawler.py` | 50000字符截断 | ✅ 已修复 |

---

## 第六章：代码审查清单

在提交代码前，请逐项检查：

### 6.1 证据处理检查

- [ ] 是否对证据内容进行了字符数截断？
- [ ] 是否只使用了证据摘要而非完整内容？
- [ ] 是否限制了处理的证据数量？
- [ ] 证据内容是否被"..."或"[内容截断]"替换？

### 6.2 正确的代码模式

```python
# ✅ 正确：直接使用内容
evidence.extracted_content
evidence.raw_content
evidence.summary  # 如果summary已被修复为完整内容

# ❌ 错误：任何形式的截断
content[:N]
summary[:N]
evidence[:N]
```

---

## 第七章：违规处理

### 7.1 代码审查

所有涉及证据处理的代码变更，必须经过以下检查：

1. **自检**：开发者对照本章检查清单自检
2. **同行评审**：至少一名其他开发者确认
3. **完整性测试**：使用长文本（>50000字符）进行测试

### 7.2 发现违规

如果发现违规代码：
1. 立即修复，不得以任何理由推迟
2. 记录违规原因和改进措施
3. 在团队会议中分享经验教训

---

## 附录：术语表

| 术语 | 定义 |
|-----|-----|
| 证据完整性 | 证据的全部字符内容在处理、存储、传递过程中保持不变 |
| 截断 | 对文本内容进行长度限制，丢弃超过限制的部分 |
| 摘要 | 对长文本进行压缩提取，只保留核心信息 |
| 完整性优先 | 在证据处理中，完整性优于效率和其他考虑 |

---

## 第八章：证据AI分类原则

### 8.1 核心共识

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                          │
│   ⚠️  证据分类的核心原则                                            │
│                                                                          │
│   1. 每个证据都需要AI逐字逐句分析理解                               │
│   2. 不使用关键词触发进行证据分类                                    │
│   3. AI自动判断证据性质，归类到正确标签                             │
│   4. 避免重复分析（已分类的证据不再重复发现）                         │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 8.2 禁止的分类方式

| 禁止方式 | 错误示例 | 正确做法 |
|---------|---------|---------|
| 关键词匹配 | 内容包含"函"字→归类为函件 | AI逐字逐句理解后判断 |
| 规则触发 | 包含"合同"→归类为合同 | AI理解文档性质后分类 |
| 正则匹配 | 匹配到letter→归类为CORRESPONDENCE | LLM分析内容语义 |

### 8.3 允许的分类方式

| 分类方式 | 实现要求 |
|---------|---------|
| LLM分析 | 调用LLM服务，逐字逐句理解证据内容 |
| 语义判断 | AI判断文档性质而非关键词匹配 |
| 自动归类 | 分析完成后自动分发到对应标签页 |

### 8.4 证据类型定义

| 类型 | 定义 | 示例 |
|-----|-----|-----|
| CONTRACT | 合同协议类 | 正式合同、协议、补充协议，有签字盖章 |
| CORRESPONDENCE | 函件文书类 | 信函、邮件、通知、催告函 |
| PAYMENT | 付款凭证类 | 发票、收据、转账记录 |
| IDENTITY | 身份证明类 | 身份证、营业执照、委托书 |
| AUDIO_VIDEO | 视听资料类 | 录音、录像、视频 |
| TESTIMONY | 证人证言类 | 证人证言、笔录 |
| EXPERT | 鉴定评估类 | 鉴定报告、评估报告 |
| DOCUMENT | 政府文件类 | 证书、批复、决定 |
| OTHER | 其他 | 不属于以上类型 |

### 8.5 自动创建关联记录

当AI判断证据类型为特定类型时，自动创建关联记录：

| 证据类型 | 自动创建 | 说明 |
|---------|--------|------|
| CORRESPONDENCE | 自动创建Letter记录 | 避免重复点击"AI发现涵件" |
| CONTRACT | 关联到合同管理 | 自动标记合同类证据 |
| PAYMENT | 关联到付款跟踪 | 自动标记付款凭证 |

---

## 第九章：文书对话栏目设计

### 9.1 核心理念

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                          │
│   ⚠️  文书对话栏目设计原则                                        │
│                                                                          │
│   1. 把案件比作战争                                              │
│   2. 把诉求比作战役                                              │
│   3. AI既是战略大师分配资源（证据），又是将军打好每一场战役              │
│   4. 案件数据统一读取（选择案件后所有栏目从该案件读取）              │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 9.2 战役模型

| 字段 | 说明 |
|-----|------|
| title | 战役名称 |
| description | 战役目标 |
| claim_type | 战役类型（确认/违约/追偿等） |
| amount | 涉及金额 |
| priority | 优先级 |
| status | 状态（进行中/已完成） |
| required_evidence_ids | 所需证据ID列表 |
| required_documents | 所需文书列表 |

### 9.3 流程设计

```
案件分析 → 识别战役 → 确认战役 → 生成文书
    ↓
收集证据 → 筛选证据 → 输出证据册
    ↓
时间把控 → 发送涵件 → 跟踪回复
```

### 9.4 证据册排版升级（2026-04-09）

#### 设计原则

证据册是给法官/仲裁看的，所有语句都要对案例有帮助：

| 原则 | 说明 |
|------|------|
| 表达清晰 | 每份证据有证据编号、页码 |
| 条理明白 | 目录显示：类别-编号-名称-证明事项-页码 |
| 证据分类 | 同诉求的证据放在一起 |
| 便于查阅 | 按合同协议类、函件类、付款凭证类分组 |

#### 升级内容

| 升级项 | 实现 |
|--------|------|
| 1. 图片尺寸 | 原始尺寸，超A4自动缩放，超长分页 |
| 2. CSS样式 | 防止变形，精致单线框 |
| 3. 框线格式 | 1px实线框，统一的表头背景 |
| 4. 证据分类 | 按诉求分组（L-合同类/C-函件类/P-付款类等） |
| 5. 连续页码 | 每页显示页码 |
| 6. 编号规则 | 类别缩写+序号（如：L001表示函件类第1个） |

#### 证据编号规则

```
C001-C099  合同协议类
L001-L099  函件文书类
P001-P099  付款凭证类
I001-I099  身份证明类
V001-V099  视听资料类
T001-T099  证人证言类
E001-E099  鉴定评估类
D001-D099  政府文件类
O001-O099  其他类
```

#### 图片排版规则

```
1. 保持原始比例（不拉伸变形）
2. 超宽(>190mm)：按页面宽度缩放
3. 超长：自动分页显示
4. 精致框线：1px实线
```

---

## 版本历史

| 版本 | 日期 | 修改内容 |
|-----|-----|---------|
| 1.0 | 2026-04-07 | 初始版本，建立证据完整性最高原则 |
| 1.1 | 2026-04-09 | 新增证据AI分类原则、文书对话栏目设计 |

---

**本宪法由系统自动执行，任何违反证据完整性原则的代码变更将被拒绝。**
